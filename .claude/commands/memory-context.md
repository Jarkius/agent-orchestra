# Memory Context

Get a context bundle for starting a new session.

## Instructions

Run the context command:
```bash
bun memory context $ARGUMENTS
```

If arguments provided, they're used as a search query to find relevant context.

This returns:
- Recent sessions with what_worked and learnings
- Related learnings (high confidence)
- Quick stats summary

Present the context to help the user get oriented in a new session.
