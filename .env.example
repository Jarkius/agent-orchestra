# Embedding Provider Configuration
# ================================
# Provider: "fastembed" (default) or "transformers"
EMBEDDING_PROVIDER=fastembed

# Model name (depends on provider)
# FastEmbed models: bge-small-en-v1.5, bge-base-en-v1.5, all-minilm-l6-v2
# Transformers models: bge-small-en-v1.5, nomic-embed-text-v1, nomic-embed-text-v1.5, all-minilm-l6-v2
EMBEDDING_MODEL=bge-small-en-v1.5

# Optional: Custom dimensions (for Matryoshka truncation with nomic/gemma)
# EMBEDDING_DIMS=384

# Batch size for bulk embedding operations
EMBEDDING_BATCH_SIZE=32

# Optional: Model cache directory
# EMBEDDING_CACHE_DIR=~/.cache/embeddings

# ChromaDB Configuration
# ======================
CHROMA_URL=http://localhost:8000

# Legacy FastEmbed config (still supported for backwards compatibility)
# FASTEMBED_MODEL=BGESmallENV15
# FASTEMBED_CACHE_DIR=
# FASTEMBED_BATCH_SIZE=32
